services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    volumes:
      - outlinergod-data:/data
    ports:
      - "3000:3000"
    env_file: .env
    restart: unless-stopped

volumes:
  outlinergod-data:
