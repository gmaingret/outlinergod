services:
  backend:
    build:
      context: ./backend
    volumes:
      - outlinergod-data:/data
    ports:
      - "3000:3000"
    env_file: .env
    restart: unless-stopped

volumes:
  outlinergod-data:
