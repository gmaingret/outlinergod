---
phase: 23-data-model-structure
plan: 04
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - android/app/src/main/java/com/gmaingret/outlinergod/db/AppDatabase.kt
  - android/app/build.gradle.kts
  - android/app/schemas/com.gmaingret.outlinergod.db.AppDatabase/3.json
autonomous: true
requirements: [SC-4]

must_haves:
  truths:
    - "AppDatabase @Database annotation has exportSchema = true"
    - "android/app/build.gradle.kts has a top-level ksp { arg(\"room.schemaLocation\", ...) } block"
    - "android/app/schemas/com.gmaingret.outlinergod.db.AppDatabase/3.json exists and is committed"
    - "AppDatabase.buildInMemory() still passes its existing tests after exportSchema change"
    - "All existing tests still pass"
  artifacts:
    - path: "android/app/schemas/com.gmaingret.outlinergod.db.AppDatabase/3.json"
      provides: "Room-generated schema snapshot for version 3"
      contains: "attachment_url"
    - path: "android/app/build.gradle.kts"
      provides: "KSP schema export arg"
      contains: "room.schemaLocation"
  key_links:
    - from: "build.gradle.kts ksp block"
      to: "android/app/schemas/"
      via: "arg(\"room.schemaLocation\", \"$projectDir/schemas\")"
      pattern: "room.schemaLocation"
---

<objective>
Enable Room schema export: set exportSchema = true in AppDatabase, add the KSP schemaLocation argument to build.gradle.kts, generate the 3.json schema file, and commit it. This makes Room validate schema integrity at build time.

Purpose: Enables Room schema diffing and migration validation; required by Phase 24 migration test coverage.
Output: exportSchema = true in AppDatabase, ksp arg in build.gradle.kts, committed 3.json schema file.

Note: This plan depends on Plan 01 because 3.json must reflect version 3 (which includes attachment_url and attachment_mime columns added by MIGRATION_2_3).
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-data-model-structure/23-CONTEXT.md
@.planning/phases/23-data-model-structure/23-RESEARCH.md
@.planning/phases/23-data-model-structure/23-01-SUMMARY.md
</context>

<interfaces>
<!-- Current state verified by source inspection -->

Current AppDatabase.kt header:
```kotlin
@Database(
    entities = [NodeEntity::class, DocumentEntity::class, BookmarkEntity::class, SettingsEntity::class],
    version = 2,           // ← Plan 01 changes this to 3
    exportSchema = false   // ← this plan changes to true
)
```
(After Plan 01 runs: version = 3, exportSchema = false)

Current build.gradle.kts (relevant section):
- Uses Kotlin DSL (.kts extension)
- Has `ksp(libs.room.compiler)` in dependencies block (line 95)
- NO existing `ksp { }` block at top level
- KSP settings are in gradle.properties (ksp.incremental=false) — not in build.gradle.kts

Target ksp block for build.gradle.kts:
```kotlin
ksp {
    arg("room.schemaLocation", "$projectDir/schemas")
}
```
Place this as a top-level block in build.gradle.kts (at the same level as `android { }`, `dependencies { }`, etc.).
Do NOT use Groovy syntax — this is a .kts file (Kotlin DSL).

Schema output path: android/app/schemas/com.gmaingret.outlinergod.db.AppDatabase/3.json
Room KSP generates this automatically on the first build after exportSchema = true + schemaLocation are set.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Enable exportSchema = true and add ksp schemaLocation arg</name>
  <files>
    android/app/src/main/java/com/gmaingret/outlinergod/db/AppDatabase.kt
    android/app/build.gradle.kts
  </files>
  <action>
    1. In AppDatabase.kt, change `exportSchema = false` to `exportSchema = true`.
       (version should already be 3 from Plan 01. Verify before editing.)

    2. In android/app/build.gradle.kts, add a top-level ksp block.
       Read build.gradle.kts first to find the right insertion point — place after the `dependencies { }` block or at the end of the file, as a peer to other top-level blocks:
       ```kotlin
       ksp {
           arg("room.schemaLocation", "$projectDir/schemas")
       }
       ```
       Kotlin DSL note: `$projectDir` is a standard Gradle property — no import needed.

    3. Run kspDebugKotlin to trigger schema generation:
       export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd /c/Users/gmain/Dev/OutlinerGod/android && ./gradlew kspDebugKotlin

    4. Verify schema JSON was generated:
       `ls android/app/schemas/com.gmaingret.outlinergod.db.AppDatabase/`
       Expected: `3.json` exists.

    5. Confirm 3.json contains the attachment_url column:
       `grep "attachment_url" android/app/schemas/com.gmaingret.outlinergod.db.AppDatabase/3.json`
       Expected: match found.

    6. If the schemas/ directory was created by the build, stage it for commit:
       `git add android/app/schemas/`
       (The git commit is handled by the executor workflow — just stage the file.)
  </action>
  <verify>
    <automated>ls /c/Users/gmain/Dev/OutlinerGod/android/app/schemas/com.gmaingret.outlinergod.db.AppDatabase/3.json && grep "attachment_url" /c/Users/gmain/Dev/OutlinerGod/android/app/schemas/com.gmaingret.outlinergod.db.AppDatabase/3.json</automated>
  </verify>
  <done>3.json exists under android/app/schemas/com.gmaingret.outlinergod.db.AppDatabase/. File contains attachment_url. AppDatabase.kt has exportSchema = true. build.gradle.kts has the ksp { arg(...) } block.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify no exportSchema regression</name>
  <files></files>
  <action>
    Run the full Android test suite to confirm exportSchema = true did not break buildInMemory() or any existing test:

    export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd /c/Users/gmain/Dev/OutlinerGod/android && ./gradlew testDebugUnitTest 2>&1 | tail -30

    Known pitfall: AppDatabaseMigrationTest.databaseVersion_is3() was updated in Plan 01 to assert version 3. Confirm it still passes.

    If the build fails with "Schema export directory does not exist" or similar:
    - Create the directory manually: `mkdir -p android/app/schemas`
    - Re-run ./gradlew kspDebugKotlin
    - Then re-run testDebugUnitTest

    The flaky test BookmarkDaoTest.observeAllActive_excludesOtherUsers (pre-existing Robolectric race) may appear — note it but do not treat as a new failure if it was already flaky.
  </action>
  <verify>
    <automated>export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd /c/Users/gmain/Dev/OutlinerGod/android && ./gradlew testDebugUnitTest 2>&1 | tail -20</automated>
  </verify>
  <done>testDebugUnitTest exits 0 (or with only the pre-existing flaky test). All migration tests including databaseVersion_is3 pass. No new failures introduced by exportSchema = true.</done>
</task>

</tasks>

<verification>
Full suite must be green:
export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd /c/Users/gmain/Dev/OutlinerGod/android && ./gradlew testDebugUnitTest

Schema file check: ls android/app/schemas/com.gmaingret.outlinergod.db.AppDatabase/3.json

Content check: grep "attachment_url" android/app/schemas/com.gmaingret.outlinergod.db.AppDatabase/3.json → match found
</verification>

<success_criteria>
1. AppDatabase.kt has exportSchema = true
2. build.gradle.kts has ksp { arg("room.schemaLocation", ...) } block (Kotlin DSL)
3. android/app/schemas/com.gmaingret.outlinergod.db.AppDatabase/3.json exists and contains attachment_url
4. 3.json is staged/committed under android/app/schemas/
5. All ~225 existing tests pass
</success_criteria>

<output>
After completion, create .planning/phases/23-data-model-structure/23-04-SUMMARY.md
</output>
