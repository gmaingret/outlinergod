---
phase: 23-data-model-structure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/gmaingret/outlinergod/util/FractionalIndex.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/prototype/FractionalIndex.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModel.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListScreen.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/prototype/DndPrototypeViewModel.kt
  - backend/src/hlc.ts
autonomous: true
requirements: [SC-2, SC-3]

must_haves:
  truths:
    - "FractionalIndex.kt lives at util/FractionalIndex.kt with package com.gmaingret.outlinergod.util"
    - "prototype/FractionalIndex.kt is deleted — no stub, no re-export"
    - "All production import sites (NodeEditorViewModel, DocumentListScreen) import from util package"
    - "DndPrototypeViewModel import updated to util package (stays in prototype/ directory)"
    - "backend/src/hlc.ts (old hex-format file) is deleted"
    - "backend/src/hlc/hlc.ts still exists"
    - "All existing tests still pass"
  artifacts:
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/util/FractionalIndex.kt"
      provides: "FractionalIndex object in util package"
      contains: "package com.gmaingret.outlinergod.util"
  key_links:
    - from: "NodeEditorViewModel.kt"
      to: "util/FractionalIndex.kt"
      via: "import com.gmaingret.outlinergod.util.FractionalIndex"
      pattern: "import.*util.FractionalIndex"
    - from: "DocumentListScreen.kt"
      to: "util/FractionalIndex.kt"
      via: "import com.gmaingret.outlinergod.util.FractionalIndex"
      pattern: "import.*util.FractionalIndex"
---

<objective>
Two structural cleanup tasks run in parallel concern: (1) move FractionalIndex from prototype/ to util/ and update all import sites, (2) delete the orphan backend/src/hlc.ts. Both are mechanical — no logic changes.

Purpose: Eliminates production imports from the prototype namespace; removes dead code from the backend.
Output: util/FractionalIndex.kt, deleted prototype/FractionalIndex.kt, deleted backend/src/hlc.ts, updated imports in 3 files.
</objective>

<execution_context>
@C:/Users/gmain/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gmain/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-data-model-structure/23-CONTEXT.md
@.planning/phases/23-data-model-structure/23-RESEARCH.md
</context>

<interfaces>
<!-- Key facts about files being changed. -->

FractionalIndex import sites confirmed by grep:
- android/.../ui/screen/nodeeditor/NodeEditorViewModel.kt line 14:
    `import com.gmaingret.outlinergod.prototype.FractionalIndex`
- android/.../ui/screen/documentlist/DocumentListScreen.kt line 62:
    `import com.gmaingret.outlinergod.prototype.FractionalIndex`
- android/.../prototype/DndPrototypeViewModel.kt:
    uses FractionalIndex (unqualified — same package); after move must import explicitly from util

FlatNodeMapper.kt does NOT import FractionalIndex (confirmed by grep — skip it).

No test files import FractionalIndex from prototype (confirmed by grep).

backend/src/hlc.ts — confirmed exists and is not imported by anything in backend/src/
backend/src/hlc/hlc.ts — confirmed exists (production file, do NOT touch)

util/ directory already exists at:
  android/app/src/main/java/com/gmaingret/outlinergod/util/
  (currently contains SyncLogger.kt)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move FractionalIndex to util/ and update all import sites</name>
  <files>
    android/app/src/main/java/com/gmaingret/outlinergod/util/FractionalIndex.kt
    android/app/src/main/java/com/gmaingret/outlinergod/prototype/FractionalIndex.kt
    android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModel.kt
    android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListScreen.kt
    android/app/src/main/java/com/gmaingret/outlinergod/prototype/DndPrototypeViewModel.kt
  </files>
  <action>
    1. Read android/app/src/main/java/com/gmaingret/outlinergod/prototype/FractionalIndex.kt in full.

    2. Create android/app/src/main/java/com/gmaingret/outlinergod/util/FractionalIndex.kt with:
       - Package declaration changed to: `package com.gmaingret.outlinergod.util`
       - All other code identical to the prototype version

    3. Delete android/app/src/main/java/com/gmaingret/outlinergod/prototype/FractionalIndex.kt — overwrite with empty is not acceptable; use Bash `rm` to remove the file:
       `rm /c/Users/gmain/Dev/OutlinerGod/android/app/src/main/java/com/gmaingret/outlinergod/prototype/FractionalIndex.kt`

    4. In NodeEditorViewModel.kt, change line 14:
       FROM: `import com.gmaingret.outlinergod.prototype.FractionalIndex`
       TO:   `import com.gmaingret.outlinergod.util.FractionalIndex`

    5. In DocumentListScreen.kt, change line 62:
       FROM: `import com.gmaingret.outlinergod.prototype.FractionalIndex`
       TO:   `import com.gmaingret.outlinergod.util.FractionalIndex`

    6. In DndPrototypeViewModel.kt, add explicit import (it was previously resolving FractionalIndex from the same package):
       Add at the top of the imports section:
       `import com.gmaingret.outlinergod.util.FractionalIndex`

    Note: Do NOT touch FlatNodeMapper.kt, test files, or any other prototype/ files. Only FractionalIndex.kt is moved.
  </action>
  <verify>
    <automated>export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd /c/Users/gmain/Dev/OutlinerGod/android && ./gradlew assembleDebug 2>&1 | tail -20</automated>
  </verify>
  <done>assembleDebug exits 0. util/FractionalIndex.kt exists with package com.gmaingret.outlinergod.util. prototype/FractionalIndex.kt does not exist. grep for "prototype.FractionalIndex" in src/main/ returns zero results.</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphan backend/src/hlc.ts and verify suite</name>
  <files>
    backend/src/hlc.ts
  </files>
  <action>
    1. Confirm backend/src/hlc.ts exists and is the old hex-format file (Phase 0 prototype):
       `cat /c/Users/gmain/Dev/OutlinerGod/backend/src/hlc.ts | head -10`

    2. Confirm nothing imports it:
       `grep -r "from.*['\"].*src/hlc['\"]" /c/Users/gmain/Dev/OutlinerGod/backend/src/ 2>/dev/null`
       Expected: no output (safe to delete).

    3. Delete the file:
       `rm /c/Users/gmain/Dev/OutlinerGod/backend/src/hlc.ts`

    4. Confirm backend/src/hlc/hlc.ts still exists:
       `ls /c/Users/gmain/Dev/OutlinerGod/backend/src/hlc/hlc.ts`

    5. Run backend tests to confirm no regression:
       `cd /c/Users/gmain/Dev/OutlinerGod/backend && pnpm test`

    6. Run full Android test suite to confirm no regression from FractionalIndex move:
       `export JAVA_HOME="..." && export ANDROID_HOME="..." && cd android && ./gradlew testDebugUnitTest`
  </action>
  <verify>
    <automated>ls /c/Users/gmain/Dev/OutlinerGod/backend/src/hlc.ts 2>&1 | grep -q "No such" && echo "DELETED OK" && ls /c/Users/gmain/Dev/OutlinerGod/backend/src/hlc/hlc.ts && echo "PRODUCTION HLC OK"</automated>
  </verify>
  <done>backend/src/hlc.ts does not exist. backend/src/hlc/hlc.ts exists. Backend pnpm test passes. Android ./gradlew testDebugUnitTest passes.</done>
</task>

</tasks>

<verification>
Full Android suite after plan:
export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd /c/Users/gmain/Dev/OutlinerGod/android && ./gradlew testDebugUnitTest

Backend suite: cd /c/Users/gmain/Dev/OutlinerGod/backend && pnpm test

Manual checks:
- grep -r "prototype.FractionalIndex" android/app/src/main/ → zero results
- ls backend/src/hlc.ts → "No such file or directory"
- ls backend/src/hlc/hlc.ts → file exists
</verification>

<success_criteria>
1. util/FractionalIndex.kt exists with package com.gmaingret.outlinergod.util
2. prototype/FractionalIndex.kt does not exist
3. No production file imports from com.gmaingret.outlinergod.prototype.FractionalIndex
4. backend/src/hlc.ts does not exist; backend/src/hlc/hlc.ts does exist
5. All ~225 Android tests pass; all backend tests pass
</success_criteria>

<output>
After completion, create .planning/phases/23-data-model-structure/23-02-SUMMARY.md
</output>
