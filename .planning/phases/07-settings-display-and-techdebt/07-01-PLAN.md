---
phase: 07-settings-display-and-techdebt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/gmaingret/outlinergod/MainActivity.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/db/entity/SyncStatus.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncScheduler.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncConstants.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncWorker.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModel.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModel.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/sync/SyncWorkerTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListSyncTest.kt
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "When a user changes theme or density in SettingsScreen, the change is immediately visible in the app UI"
    - "No orphaned db/entity/SyncStatus.kt file exists in the codebase"
    - "SyncScheduler uses ExistingPeriodicWorkPolicy.UPDATE (not deprecated KEEP or REPLACE)"
    - "LAST_SYNC_HLC_KEY is defined once in a shared constant, not duplicated across 3 companion objects"
  artifacts:
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/MainActivity.kt"
      provides: "Settings observation using getUserId() instead of getAccessToken()"
      contains: "authRepository.getUserId()"
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncConstants.kt"
      provides: "Shared LAST_SYNC_HLC_KEY constant"
      contains: "LAST_SYNC_HLC_KEY"
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncScheduler.kt"
      provides: "Updated WorkManager policy"
      contains: "ExistingPeriodicWorkPolicy.UPDATE"
  key_links:
    - from: "android/app/src/main/java/com/gmaingret/outlinergod/MainActivity.kt"
      to: "authRepository.getUserId()"
      via: "flatMapLatest on getUserId() flow"
      pattern: "getUserId\\(\\)\\.flatMapLatest"
    - from: "android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncWorker.kt"
      to: "android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncConstants.kt"
      via: "import SyncConstants.LAST_SYNC_HLC_KEY"
      pattern: "SyncConstants\\.LAST_SYNC_HLC_KEY"
---

<objective>
Close GAP-A from the v0.4 re-audit and fix 3 non-blocking tech debt items.

Purpose: GAP-A causes settings changes (theme, density, guide lines, backlink badge) to never visually apply because MainActivity passes a JWT token string as the userId to settingsDao.getSettings(), which never matches the UUID-keyed settings rows. The 3 tech debt items are: orphaned db/entity/SyncStatus.kt enum, SyncScheduler using deprecated KEEP policy, and LAST_SYNC_HLC_KEY duplicated in 3 companion objects.

Output: All 4 fixes applied, all 225+ existing tests pass, GAP-A closed.
</objective>

<execution_context>
@C:\Users\gmain\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\gmain\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v0.4-MILESTONE-AUDIT.md
@android/app/src/main/java/com/gmaingret/outlinergod/MainActivity.kt
@android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncScheduler.kt
@android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncWorker.kt
@android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModel.kt
@android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModel.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix MainActivity getUserId() and delete orphaned SyncStatus enum</name>
  <files>
    android/app/src/main/java/com/gmaingret/outlinergod/MainActivity.kt
    android/app/src/main/java/com/gmaingret/outlinergod/db/entity/SyncStatus.kt
  </files>
  <action>
    **MainActivity.kt — Fix GAP-A (lines 31-33):**

    Change the settingsFlow definition from:
    ```kotlin
    val settingsFlow = authRepository.getAccessToken().flatMapLatest { accessToken ->
        if (accessToken != null) settingsDao.getSettings(accessToken) else flowOf(null)
    }
    ```
    To:
    ```kotlin
    val settingsFlow = authRepository.getUserId().flatMapLatest { userId ->
        if (userId != null) settingsDao.getSettings(userId) else flowOf(null)
    }
    ```

    This is the ONLY change in MainActivity.kt. The lambda parameter name changes from `accessToken` to `userId` for clarity, and the method call changes from `getAccessToken()` to `getUserId()`. The rest of the file (imports, theme logic, density logic) stays identical. No new imports needed — `AuthRepository` is already imported and `getUserId()` is on the same interface.

    **db/entity/SyncStatus.kt — Delete the file entirely.**

    This file contains `enum class SyncStatus { SYNCED, PENDING }` and is confirmed NOT imported anywhere in the codebase (grep verified zero matches for `import com.gmaingret.outlinergod.db.entity.SyncStatus`). The ACTIVE SyncStatus enum lives at `ui/common/SyncStatus.kt` (`enum class SyncStatus { Idle, Syncing, Error }`) and is unrelated. Simply delete `db/entity/SyncStatus.kt`.
  </action>
  <verify>
    1. Grep the codebase for `getAccessToken` in MainActivity.kt — must return zero matches
    2. Grep for `getUserId()` in MainActivity.kt — must return exactly one match (line 31)
    3. Confirm `db/entity/SyncStatus.kt` no longer exists on disk
    4. Grep for `import com.gmaingret.outlinergod.db.entity.SyncStatus` — must return zero matches
    5. Run `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd android && ./gradlew test` — all 225+ tests pass
  </verify>
  <done>
    - MainActivity.kt line 31 calls `authRepository.getUserId()` (not `getAccessToken()`)
    - The UUID from getUserId() is passed to `settingsDao.getSettings(userId)`, matching settings rows written by SettingsViewModel
    - `db/entity/SyncStatus.kt` file deleted from disk
    - All existing tests pass (no test referenced the deleted enum or the old MainActivity code path)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix SyncScheduler policy and extract LAST_SYNC_HLC_KEY to shared constant</name>
  <files>
    android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncConstants.kt
    android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncScheduler.kt
    android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncWorker.kt
    android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModel.kt
    android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModel.kt
    android/app/src/test/java/com/gmaingret/outlinergod/sync/SyncWorkerTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListSyncTest.kt
  </files>
  <action>
    **1. Create `SyncConstants.kt` in the `sync` package:**

    Create a new file at `android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncConstants.kt`:
    ```kotlin
    package com.gmaingret.outlinergod.sync

    import androidx.datastore.preferences.core.stringPreferencesKey

    object SyncConstants {
        val LAST_SYNC_HLC_KEY = stringPreferencesKey("last_sync_hlc")
    }
    ```

    **2. Update SyncScheduler.kt (line 29):**

    Change `ExistingPeriodicWorkPolicy.KEEP` to `ExistingPeriodicWorkPolicy.UPDATE` on line 29. This is the only change in SyncScheduler.kt. The import on line 5 (`import androidx.work.ExistingPeriodicWorkPolicy`) stays the same — `UPDATE` is on the same enum.

    **3. Update SyncWorker.kt — remove companion LAST_SYNC_HLC_KEY:**

    - Remove line 109: `internal val LAST_SYNC_HLC_KEY = stringPreferencesKey("last_sync_hlc")` from the companion object
    - If the companion object becomes empty after removal, remove the entire companion object block (lines 108-110)
    - Remove the import for `stringPreferencesKey` if it's no longer used in this file (it was only used for the companion val). Check first — if other code in SyncWorker uses `stringPreferencesKey`, keep the import.
    - Add import: `import com.gmaingret.outlinergod.sync.SyncConstants` (or since SyncWorker is already in the `sync` package, the unqualified `SyncConstants` will resolve)
    - Change all usages of `LAST_SYNC_HLC_KEY` (lines 49 and 102) to `SyncConstants.LAST_SYNC_HLC_KEY`
    - Add import for `stringPreferencesKey` removal check: the only usage was the companion val. Lines 49 and 102 use `prefs[LAST_SYNC_HLC_KEY]` and `prefs[LAST_SYNC_HLC_KEY] = ...` which reference the key, not `stringPreferencesKey` directly. So the `stringPreferencesKey` import can be removed from SyncWorker.kt.

    **4. Update DocumentListViewModel.kt — remove companion LAST_SYNC_HLC_KEY:**

    - Remove line 249: `internal val LAST_SYNC_HLC_KEY = stringPreferencesKey("last_sync_hlc")` from the companion object
    - If companion becomes empty, remove the entire companion block (lines 248-250)
    - Remove the `stringPreferencesKey` import if no longer used in this file
    - Add import: `import com.gmaingret.outlinergod.sync.SyncConstants`
    - Change usages at lines 79 and 131 from `LAST_SYNC_HLC_KEY` to `SyncConstants.LAST_SYNC_HLC_KEY`

    **5. Update NodeEditorViewModel.kt — remove companion LAST_SYNC_HLC_KEY:**

    - Remove line 718: `internal val LAST_SYNC_HLC_KEY = stringPreferencesKey("last_sync_hlc")` from the companion object
    - Do NOT remove the companion object — it still contains `recomputeFlatNodes()` and other functions (line 720+)
    - Remove the `stringPreferencesKey` import if no longer used in this file
    - Add import: `import com.gmaingret.outlinergod.sync.SyncConstants`
    - Change usages at lines 638 and 687 from `LAST_SYNC_HLC_KEY` to `SyncConstants.LAST_SYNC_HLC_KEY`

    **6. Update test files to reference SyncConstants:**

    - In `SyncWorkerTest.kt` line 197: change `SyncWorker.LAST_SYNC_HLC_KEY` to `SyncConstants.LAST_SYNC_HLC_KEY`. Add import `import com.gmaingret.outlinergod.sync.SyncConstants`.
    - In `DocumentListSyncTest.kt` line 171: change `DocumentListViewModel.LAST_SYNC_HLC_KEY` to `SyncConstants.LAST_SYNC_HLC_KEY`. Add import `import com.gmaingret.outlinergod.sync.SyncConstants`.
  </action>
  <verify>
    1. Grep for `stringPreferencesKey("last_sync_hlc")` across the codebase — must return exactly ONE match, in `SyncConstants.kt`
    2. Grep for `LAST_SYNC_HLC_KEY` — all matches should reference `SyncConstants.LAST_SYNC_HLC_KEY` (no standalone `LAST_SYNC_HLC_KEY` definitions in companion objects)
    3. Grep for `ExistingPeriodicWorkPolicy.KEEP` — must return zero matches
    4. Grep for `ExistingPeriodicWorkPolicy.UPDATE` — must return exactly one match in SyncScheduler.kt
    5. Confirm `SyncConstants.kt` exists at `android/app/src/main/java/com/gmaingret/outlinergod/sync/SyncConstants.kt`
    6. Run `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd android && ./gradlew test` — all 225+ tests pass
  </verify>
  <done>
    - `SyncConstants.kt` exists with single `LAST_SYNC_HLC_KEY` definition
    - SyncWorker, DocumentListViewModel, NodeEditorViewModel all import and use `SyncConstants.LAST_SYNC_HLC_KEY` (no local definitions)
    - SyncWorkerTest and DocumentListSyncTest reference `SyncConstants.LAST_SYNC_HLC_KEY`
    - SyncScheduler uses `ExistingPeriodicWorkPolicy.UPDATE` (not KEEP or REPLACE)
    - All 225+ existing tests pass
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **GAP-A closed:** `MainActivity.kt` uses `getUserId()` to query settings, meaning SettingsScreen writes (which use `getUserId()`) are now readable by MainActivity. Theme/density changes will visually apply.
2. **Dead code removed:** `db/entity/SyncStatus.kt` deleted. The active `ui/common/SyncStatus.kt` is unaffected.
3. **WorkManager policy fixed:** `ExistingPeriodicWorkPolicy.UPDATE` is the modern replacement, preserving the current period while allowing constraint/param updates.
4. **LAST_SYNC_HLC_KEY deduplicated:** Single source of truth in `SyncConstants.kt`. All 3 consumers import from there. No race-condition behavior change (the concurrent access pattern remains unchanged), but the code is now DRY and any future key rename is a single edit.
5. **All tests green:** `./gradlew test` passes 225+ tests (1 known pre-existing flaky BookmarkDaoTest may fail on first run, passes on retry).
</verification>

<success_criteria>
- [ ] `MainActivity.kt` line 31 calls `authRepository.getUserId()` not `getAccessToken()`
- [ ] `db/entity/SyncStatus.kt` file does not exist
- [ ] `SyncScheduler.kt` uses `ExistingPeriodicWorkPolicy.UPDATE`
- [ ] `SyncConstants.kt` exists with sole `LAST_SYNC_HLC_KEY` definition
- [ ] No companion objects in SyncWorker, DocumentListViewModel, or NodeEditorViewModel contain `LAST_SYNC_HLC_KEY`
- [ ] `./gradlew test` passes (225+ tests, 0 failures excluding known flaky)
</success_criteria>

<output>
After completion, create `.planning/phases/07-settings-display-and-techdebt/07-01-SUMMARY.md`
</output>
