---
phase: 06-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListScreen.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListScreenTest.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/db/entity/NodeEntity.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/db/entity/DocumentEntity.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/db/entity/BookmarkEntity.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/db/entity/EntitySchemaTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/mapper/FlatNodeMapperTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModelTest.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModel.kt
autonomous: true

must_haves:
  truths:
    - "generateNextSortOrder uses FractionalIndex.generateKeyBetween, not string concatenation"
    - "Empty document list produces sort order 'aP' (FractionalIndex midpoint), not 'a0'"
    - "Creating multiple documents in sequence produces strictly increasing, correctly sorted keys"
    - "NodeEntity, DocumentEntity, BookmarkEntity no longer have a syncStatus field"
    - "All tests pass after syncStatus removal (EntitySchemaTest updated, test helpers updated)"
    - "ViewModel SyncStatus enum (ui/common/SyncStatus.kt) is untouched and still functions"
  artifacts:
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListScreen.kt"
      provides: "Fixed generateNextSortOrder using FractionalIndex"
      contains: "FractionalIndex.generateKeyBetween"
    - path: "android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListScreenTest.kt"
      provides: "Tests for generateNextSortOrder (empty, single, multiple, sequential ordering)"
      min_lines: 50
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/db/entity/NodeEntity.kt"
      provides: "NodeEntity without syncStatus field"
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/db/entity/DocumentEntity.kt"
      provides: "DocumentEntity without syncStatus field"
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/db/entity/BookmarkEntity.kt"
      provides: "BookmarkEntity without syncStatus field"
    - path: "android/app/src/test/java/com/gmaingret/outlinergod/db/entity/EntitySchemaTest.kt"
      provides: "Updated schema test without syncStatus assertions"
  key_links:
    - from: "DocumentListScreen.kt"
      to: "prototype/FractionalIndex.kt"
      via: "import and function call"
      pattern: "FractionalIndex\\.generateKeyBetween"
---

<objective>
Fix the sort-order divergence in DocumentListScreen and remove the dead syncStatus field from all Room entities.

Purpose: Close two code-level tech debt items from the v0.4 milestone audit. The sort-order fix ensures new documents use the same FractionalIndex alphabet as nodes (preventing merge ordering issues across devices). The syncStatus removal eliminates a dead field that was never read by any DAO query or sync DTO.

Output: Fixed generateNextSortOrder function with tests, clean entity definitions without syncStatus, updated test files.
</objective>

<execution_context>
@C:\Users\gmain\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\gmain\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tech-debt-cleanup/06-RESEARCH.md
@android/app/src/main/java/com/gmaingret/outlinergod/prototype/FractionalIndex.kt
@android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListScreen.kt
@android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModel.kt
@android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListScreenTest.kt
@android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModelTest.kt
@android/app/src/main/java/com/gmaingret/outlinergod/db/entity/NodeEntity.kt
@android/app/src/main/java/com/gmaingret/outlinergod/db/entity/DocumentEntity.kt
@android/app/src/main/java/com/gmaingret/outlinergod/db/entity/BookmarkEntity.kt
@android/app/src/test/java/com/gmaingret/outlinergod/db/entity/EntitySchemaTest.kt
@android/app/src/test/java/com/gmaingret/outlinergod/ui/mapper/FlatNodeMapperTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix generateNextSortOrder to use FractionalIndex</name>
  <files>
    android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListScreen.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListScreenTest.kt
  </files>
  <action>
    In DocumentListScreen.kt (around line 387), replace the `generateNextSortOrder` function body:

    CURRENT (broken):
    ```kotlin
    internal fun generateNextSortOrder(items: List<DocumentEntity>): String {
        if (items.isEmpty()) return "a0"
        val maxSortOrder = items.maxOf { it.sortOrder }
        return maxSortOrder + "0"
    }
    ```

    REPLACE WITH:
    ```kotlin
    import com.gmaingret.outlinergod.prototype.FractionalIndex

    internal fun generateNextSortOrder(items: List<DocumentEntity>): String {
        return if (items.isEmpty()) {
            FractionalIndex.generateKeyBetween(null, null) // "aP"
        } else {
            val maxSortOrder = items.maxOf { it.sortOrder }
            FractionalIndex.generateKeyBetween(maxSortOrder, null)
        }
    }
    ```

    Add the FractionalIndex import at the top of DocumentListScreen.kt. The call site at line 108 (`val sortOrder = generateNextSortOrder(items)`) does NOT need changes.

    In DocumentListScreenTest.kt, add 4 new test methods after the existing tests. You need a `fakeDocument` helper — either import the one from DocumentListViewModelTest if accessible, or create a local one:
    ```kotlin
    private fun fakeDocument(sortOrder: String = "aP") = DocumentEntity(
        id = java.util.UUID.randomUUID().toString(),
        userId = "u",
        title = "Test",
        type = "document",
        sortOrder = sortOrder,
        createdAt = 1000L,
        updatedAt = 1000L
    )
    ```
    NOTE: After Task 2 removes the syncStatus field from DocumentEntity, this helper must NOT include `syncStatus = 0`. If you implement Task 1 first, do NOT include syncStatus in the helper. The field has a default value in the entity so omitting it compiles now; after removal it would be a compile error if included.

    Add these tests:
    1. `generateNextSortOrder_emptyList_returnsInitialKey` — asserts result equals "aP" (FractionalIndex.generateKeyBetween(null, null))
    2. `generateNextSortOrder_singleItem_returnsKeyAfterIt` — creates fakeDocument(sortOrder = "aP"), asserts result > "aP" and starts with "a"
    3. `generateNextSortOrder_multipleItems_returnsKeyAfterMax` — creates two docs ("aP", "aPP"), asserts result > "aPP"
    4. `generateNextSortOrder_sequentialCreation_maintainsSortOrder` — creates 5 documents sequentially, asserts orders == orders.sorted()
  </action>
  <verify>
    Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd android && ./gradlew test --tests "com.gmaingret.outlinergod.ui.screen.documentlist.DocumentListScreenTest"`
    All 8 tests pass (4 existing + 4 new).
  </verify>
  <done>
    generateNextSortOrder returns "aP" for empty list (not "a0"), uses FractionalIndex.generateKeyBetween for subsequent documents, and 4 new tests confirm correct behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead syncStatus field from Room entities</name>
  <files>
    android/app/src/main/java/com/gmaingret/outlinergod/db/entity/NodeEntity.kt
    android/app/src/main/java/com/gmaingret/outlinergod/db/entity/DocumentEntity.kt
    android/app/src/main/java/com/gmaingret/outlinergod/db/entity/BookmarkEntity.kt
    android/app/src/test/java/com/gmaingret/outlinergod/db/entity/EntitySchemaTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/mapper/FlatNodeMapperTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModelTest.kt
    android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModel.kt
  </files>
  <action>
    Use Option B from the research: remove the Kotlin field, leave the SQLite column in place. Room ignores unmapped columns. No schema version bump needed.

    IMPORTANT: Do NOT touch the ViewModel SyncStatus enum at `ui/common/SyncStatus.kt`. That is a DIFFERENT thing (Idle/Syncing/Error UI state indicator). Only remove the entity `syncStatus: Int = 0` field.

    Step 1 — Remove field from entities:
    - NodeEntity.kt line 31: delete `@ColumnInfo(name = "sync_status") val syncStatus: Int = 0,`
    - DocumentEntity.kt line 25: delete `@ColumnInfo(name = "sync_status") val syncStatus: Int = 0,`
    - BookmarkEntity.kt line 28: delete `@ColumnInfo(name = "sync_status") val syncStatus: Int = 0,`
    Make sure to fix any trailing comma issues after deletion.

    Step 2 — Update EntitySchemaTest.kt:
    - In `nodeEntity_tableName_isNodes()` (line 72-82): remove `"syncStatus"` from the `expectedColumns` set
    - Delete the entire `syncStatusDefaultsToZero()` test method (lines 121-132)

    Step 3 — Update test helpers that construct entities with syncStatus = 0:
    - FlatNodeMapperTest.kt line 46: remove `syncStatus = 0,` from the fakeNode helper
    - DocumentListViewModelTest.kt line 105: remove `syncStatus = 0` from the fakeDocument helper

    Step 4 — Update DocumentListViewModel.kt entity constructors:
    - Line 181: remove `syncStatus = 0` from the DocumentEntity constructor in createDocument()
    - Line 201: remove `syncStatus = 0` from the NodeEntity constructor in createDocument()

    After all removals, verify no remaining references to the entity-level syncStatus:
    - `syncStatus = 0` should appear NOWHERE in the codebase
    - `entity.syncStatus` should appear NOWHERE
    - The ViewModel `SyncStatus` enum (SyncStatus.Idle, SyncStatus.Syncing, SyncStatus.Error) references should be UNCHANGED
  </action>
  <verify>
    Run full test suite: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd android && ./gradlew test`
    All tests pass. Grep for `syncStatus = 0` in the codebase returns zero results. Grep for `SyncStatus.Idle` still returns results (ViewModel enum is alive).
  </verify>
  <done>
    syncStatus field removed from NodeEntity, DocumentEntity, BookmarkEntity. EntitySchemaTest updated (syncStatusDefaultsToZero deleted, expectedColumns updated). All test helpers and ViewModel constructors updated. Full test suite passes. ViewModel SyncStatus enum untouched.
  </done>
</task>

</tasks>

<verification>
1. `cd android && ./gradlew test` — all tests pass (222+ expected, minus 1 deleted syncStatusDefaultsToZero test, plus 4 new sort order tests = ~225 tests)
2. Grep `syncStatus = 0` in android/ — zero matches
3. Grep `SyncStatus.Idle` in android/ — still has matches (ViewModel enum alive)
4. Grep `FractionalIndex.generateKeyBetween` in DocumentListScreen.kt — matches found
5. No `"a0"` returned from generateNextSortOrder for empty list
</verification>

<success_criteria>
- generateNextSortOrder uses FractionalIndex and returns "aP" for empty list
- 4 new sort-order tests pass in DocumentListScreenTest
- syncStatus field removed from all 3 entity classes
- EntitySchemaTest compiles and passes without syncStatus references
- Full Android test suite passes (`./gradlew test` exits 0)
- ViewModel SyncStatus enum completely unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/06-tech-debt-cleanup/06-01-SUMMARY.md`
</output>
