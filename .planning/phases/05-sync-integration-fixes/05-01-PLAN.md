---
phase: 05-sync-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/gmaingret/outlinergod/sync/HlcClock.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/sync/HlcClockTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/prototype/HlcClockTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/prototype/LwwMergeTest.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/repository/AuthRepository.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/repository/impl/AuthRepositoryImpl.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/repository/AuthRepositoryTest.kt
autonomous: true

must_haves:
  truths:
    - "HlcClock.generate() produces 13-digit-decimal-wall + 5-digit-decimal-counter format matching backend hlc.ts"
    - "HlcClock.parse() correctly round-trips the new decimal format"
    - "AuthRepository exposes getUserId(): Flow<String?> returning the user UUID, not the JWT"
    - "googleSignIn() persists user.id from AuthResponse into DataStore under user_id key"
  artifacts:
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/sync/HlcClock.kt"
      provides: "Decimal HLC format matching backend"
      contains: "padStart(13"
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/repository/AuthRepository.kt"
      provides: "getUserId() interface method"
      contains: "fun getUserId"
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/repository/impl/AuthRepositoryImpl.kt"
      provides: "USER_ID_KEY DataStore persistence"
      contains: "USER_ID_KEY"
  key_links:
    - from: "HlcClock.format()"
      to: "backend hlc.ts format()"
      via: "identical 13-digit-decimal-wall + 5-digit-decimal-counter format"
      pattern: "padStart\\(13.*padStart\\(5"
    - from: "AuthRepositoryImpl.googleSignIn()"
      to: "DataStore USER_ID_KEY"
      via: "prefs[USER_ID_KEY] = response.user.id"
      pattern: "USER_ID_KEY.*response\\.user\\.id"
---

<objective>
Fix the two infrastructure-level integration blockers (GAP-1: HLC format mismatch, GAP-2: userId storage) at the source -- HlcClock and AuthRepository -- and update their direct unit tests.

Purpose: These are the foundational changes that all downstream ViewModel fixes depend on. HlcClock format must match the backend before any sync can work. AuthRepository must expose the real user UUID before any ViewModel can query Room correctly.

Output: Updated HlcClock.kt with decimal format, updated AuthRepository with getUserId(), all infrastructure tests green.
</objective>

<execution_context>
@C:\Users\gmain\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\gmain\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sync-integration-fixes/05-RESEARCH.md
@backend/src/hlc/hlc.ts
@android/app/src/main/java/com/gmaingret/outlinergod/sync/HlcClock.kt
@android/app/src/main/java/com/gmaingret/outlinergod/repository/AuthRepository.kt
@android/app/src/main/java/com/gmaingret/outlinergod/repository/impl/AuthRepositoryImpl.kt
@android/app/src/main/java/com/gmaingret/outlinergod/network/model/Auth.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert HlcClock to decimal format matching backend</name>
  <files>
    android/app/src/main/java/com/gmaingret/outlinergod/sync/HlcClock.kt
    android/app/src/test/java/com/gmaingret/outlinergod/sync/HlcClockTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/prototype/HlcClockTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/prototype/LwwMergeTest.kt
  </files>
  <action>
    Update `HlcClock.kt` companion object:

    1. `format()`: Change from hex to decimal. Use `wallMs.toString().padStart(13, '0')` for wall (not `toString(16).padStart(16, '0')`). Use `counter.toString().padStart(5, '0')` for counter (not `toString(16).padStart(4, '0')`).

    2. `parse()`: Change from hex to decimal. Use `hlc.substring(0, firstDash).toLong()` (not `toLong(16)`). Use `hlc.substring(firstDash + 1, secondDash).toInt()` (not `toInt(16)`).

    3. Update the KDoc comment at the top of the class to document the new format: `<13-digit-decimal-wall>-<5-digit-decimal-counter>-<device_id>` with example `1772370135754-00000-a1b2c3d4-5678-90ab-cdef-12345678abcd`.

    Then update `sync/HlcClockTest.kt` (Kotest StringSpec):
    - `generate_matchesFormat`: Change regex from `^[0-9a-f]{16}-[0-9a-f]{4}-d1$` to `^\d{13}-\d{5}-d1$`
    - `generate_incrementsCounter_onSameMillisecond`: Change expected counters from `"0000"/"0001"` to `"00000"/"00001"`
    - `receive_advancesWallPastIncoming`: Build incoming HLC with decimal format: `"${(t + 5000L).toString().padStart(13, '0')}-00000-other"`. Parse result wall with `result.split("-")[0].toLong()` (not `toLong(16)`).
    - `receive_resetsCounter_whenWallAdvances`: Build incoming HLC with decimal format: `"${2000L.toString().padStart(13, '0')}-00000-other"`. Change expected counter from `"0000"` to `"00000"`.
    - `compare_higherTimestamp_returnsPositive`: Build HLCs with decimal format: `"${t1.toString().padStart(13, '0')}-00000-d"` etc.
    - `compare_higherCounter_returnsPositive`: Change wall from `"0000000000000001"` to `"0000000000001"` (13 chars). Change counters from `"0000"/"0001"` to `"00000"/"00001"`.
    - `wallHex_isExactlySixteenChars`: Rename to `wallDecimal_isExactlyThirteenChars`. Change assertion to `assertEquals(13, wallPart.length)`.
    - `sortLexicographic_matchesGenerationOrder`: No change needed (format-agnostic).

    Then update `prototype/HlcClockTest.kt`: This file imports from `com.gmaingret.outlinergod.prototype` package. The prototype HlcClock is a separate class. Check if it also uses hex format. If so, leave it alone -- the prototype is a standalone proof-of-concept not used in production. Only update test assertions if they reference the production `sync.HlcClock`.

    Then update `prototype/LwwMergeTest.kt`: This uses hardcoded hex HLC strings like `"0000017b05a3a1be-0000-device-a"`. Replace ALL occurrences with equivalent decimal strings: `"1636300202430-00000-device-a"` (same wall ms value: 0x17b05a3a1be = 1636300202430). Specifically:
    - Line 67: `"0000017b05a3a1be-0000-device-a"` -> `"1636300202430-00000-device-a"`
    - Line 68: `"0000017b05a3a1be-0001-device-b"` -> `"1636300202430-00001-device-b"`
    - Line 206: `"0000017b05a3a1be-0001-device-a"` -> `"1636300202430-00001-device-a"`
    - Line 219: `"0000017b05a3a1be-0000-device-b"` -> `"1636300202430-00000-device-b"`

    IMPORTANT: The prototype LwwMergeTest uses the prototype merge functions from `com.gmaingret.outlinergod.prototype`, which compare HLC strings lexicographically. The decimal format strings must preserve the same relative ordering as the hex ones for the tests to still pass. Verify: `"1636300202430-00000-device-a" < "1636300202430-00001-device-b"` -- yes, this holds because `00000 < 00001`.

    Do NOT touch `sync/LwwMergeTest.kt` -- it uses generic strings like "AAAA"/"ZZZZ" that are format-agnostic.
  </action>
  <verify>
    Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd android && ./gradlew test --tests "com.gmaingret.outlinergod.sync.HlcClockTest" --tests "com.gmaingret.outlinergod.sync.LwwMergeTest" --tests "com.gmaingret.outlinergod.prototype.HlcClockTest" --tests "com.gmaingret.outlinergod.prototype.LwwMergeTest"`

    All tests must pass. The format regex must match `^\d{13}-\d{5}-` pattern.
  </verify>
  <done>
    HlcClock.generate() produces `1772370135754-00000-deviceId` format. HlcClock.parse() correctly parses this back to (wallMs=1772370135754, counter=0, deviceId). All HLC-related tests (sync + prototype) pass green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add getUserId() to AuthRepository and persist user UUID on login</name>
  <files>
    android/app/src/main/java/com/gmaingret/outlinergod/repository/AuthRepository.kt
    android/app/src/main/java/com/gmaingret/outlinergod/repository/impl/AuthRepositoryImpl.kt
    android/app/src/test/java/com/gmaingret/outlinergod/repository/AuthRepositoryTest.kt
  </files>
  <action>
    1. In `AuthRepository.kt` interface, add: `fun getUserId(): Flow<String?>`

    2. In `AuthRepositoryImpl.kt`:
       - Add `private val USER_ID_KEY = stringPreferencesKey("user_id")` to the companion object
       - In `googleSignIn()`, after storing tokens in `dataStore.edit`, also store: `prefs[USER_ID_KEY] = response.user.id`
       - Implement `getUserId()`: `override fun getUserId(): Flow<String?> = dataStore.data.map { prefs -> prefs[USER_ID_KEY] }`
       - In `logout()`, also clear USER_ID_KEY: `prefs.remove(USER_ID_KEY)`
       - Do NOT modify `getAccessToken()` -- the Ktor auth interceptor still needs it

    3. In `AuthRepositoryTest.kt`, add two new tests:
       - `googleSignIn_storesUserId`: After `repo.googleSignIn("id-token")`, assert `repo.getUserId().first() == "uid1"` (matching the `mockUserProfile().id`)
       - `logout_clearsUserId`: After sign-in + logout, assert `repo.getUserId().first() == null`
  </action>
  <verify>
    Run: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd android && ./gradlew test --tests "com.gmaingret.outlinergod.repository.AuthRepositoryTest"`

    All existing tests plus 2 new tests must pass.
  </verify>
  <done>
    AuthRepository.getUserId() returns Flow<String?> with the real user UUID (not the JWT). googleSignIn stores response.user.id. logout clears it. 8 tests pass (6 existing + 2 new).
  </done>
</task>

</tasks>

<verification>
1. `HlcClock.format(1772370135754L, 3, "device-1")` returns `"1772370135754-00003-device-1"` -- exact match with backend `hlc.ts` format
2. `HlcClock.parse("1772370135754-00003-device-1")` returns `Triple(1772370135754L, 3, "device-1")`
3. `AuthRepository.getUserId()` emits `"uid1"` after successful googleSignIn (not `"access-token-123"`)
4. `AuthRepository.getAccessToken()` still emits the JWT token (unchanged behavior for Ktor interceptor)
5. All tests in sync, prototype, and repository packages pass green
</verification>

<success_criteria>
- HLC format is `<13-decimal>-<5-decimal>-<deviceId>` on Android, matching backend exactly
- getUserId() returns user UUID from AuthResponse.user.id
- getAccessToken() still returns JWT (Ktor interceptor unbroken)
- All existing + new tests pass: `./gradlew test` for the affected test classes exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-sync-integration-fixes/05-01-SUMMARY.md`
</output>
