---
phase: 05-sync-integration-fixes
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModel.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModel.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/settings/SettingsViewModel.kt
  - android/app/src/main/java/com/gmaingret/outlinergod/network/model/CreateDocumentRequest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModelTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListSyncTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModelTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorSyncTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorPersistenceTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeContextMenuTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NoteEditorTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/DragAndDropTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/settings/SettingsViewModelTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/sync/SyncWorkerTest.kt
  - android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/login/LoginViewModelTest.kt
autonomous: true

must_haves:
  truths:
    - "DocumentListViewModel queries Room with real user UUID, not JWT token"
    - "NodeEditorViewModel creates root nodes with real user UUID as userId field"
    - "SettingsViewModel queries/creates settings with real user UUID"
    - "POST /api/documents sends snake_case body (parent_id, sort_order)"
    - "All 216+ existing tests still pass after migration"
  artifacts:
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/network/model/CreateDocumentRequest.kt"
      provides: "@Serializable data class with @SerialName snake_case annotations"
      contains: "@SerialName(\"parent_id\")"
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModel.kt"
      provides: "getUserId() calls instead of getAccessToken() for userId"
      contains: "getUserId()"
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModel.kt"
      provides: "getUserId() calls instead of getAccessToken() for userId"
      contains: "getUserId()"
    - path: "android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/settings/SettingsViewModel.kt"
      provides: "getUserId() calls instead of getAccessToken() for userId"
      contains: "getUserId()"
  key_links:
    - from: "DocumentListViewModel.loadDocuments()"
      to: "authRepository.getUserId()"
      via: "filterNotNull().first() for Room userId query"
      pattern: "getUserId\\(\\)\\.filterNotNull\\(\\)\\.first\\(\\)"
    - from: "DocumentListViewModel.createDocument()"
      to: "CreateDocumentRequest"
      via: "setBody(CreateDocumentRequest(...))"
      pattern: "setBody\\(CreateDocumentRequest"
    - from: "DocumentListViewModel.triggerSync()"
      to: "authRepository.getUserId()"
      via: "userId for getPendingChanges queries"
      pattern: "getUserId.*getPendingChanges"
---

<objective>
Wire getUserId() into all three ViewModels (replacing getAccessToken() misuse), fix the camelCase POST body with a @Serializable data class, and update all test fixtures to use decimal HLC format and getUserId() mocks.

Purpose: This completes the GAP-2 and GAP-3 fixes across all consumer code. After this plan, document list queries return real data, sync pushes use correct user UUIDs, and document creation sends valid snake_case to the backend.

Output: All 3 ViewModels use getUserId() for Room queries. CreateDocumentRequest sends snake_case. Full test suite green.
</objective>

<execution_context>
@C:\Users\gmain\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\gmain\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sync-integration-fixes/05-RESEARCH.md
@.planning/phases/05-sync-integration-fixes/05-01-SUMMARY.md
@android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModel.kt
@android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModel.kt
@android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/settings/SettingsViewModel.kt
@android/app/src/main/java/com/gmaingret/outlinergod/repository/AuthRepository.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace getAccessToken() with getUserId() in all ViewModels and fix POST body</name>
  <files>
    android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModel.kt
    android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModel.kt
    android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/settings/SettingsViewModel.kt
    android/app/src/main/java/com/gmaingret/outlinergod/network/model/CreateDocumentRequest.kt
  </files>
  <action>
    **DocumentListViewModel.kt** -- 3 call sites to fix:

    1. `loadDocuments()` line ~65: Change `authRepository.getAccessToken().filterNotNull().first()` to `authRepository.getUserId().filterNotNull().first()`

    2. `triggerSync()` line ~110: Change `authRepository.getAccessToken().filterNotNull().first()` to `authRepository.getUserId().filterNotNull().first()` (the userId used for getPendingChanges)

    3. `createDocument()` line ~169: Change `authRepository.getAccessToken().filterNotNull().first()` to `authRepository.getUserId().filterNotNull().first()`

    4. `createDocument()` lines ~194-204: Replace the `mapOf(...)` POST body with a `@Serializable` data class. Change:
    ```kotlin
    setBody(mapOf(
        "id" to doc.id,
        "title" to doc.title,
        "type" to doc.type,
        "parentId" to doc.parentId,
        "sortOrder" to doc.sortOrder
    ))
    ```
    To:
    ```kotlin
    setBody(CreateDocumentRequest(
        id = doc.id,
        title = doc.title,
        type = doc.type,
        parentId = doc.parentId,
        sortOrder = doc.sortOrder
    ))
    ```
    Add the import for `CreateDocumentRequest`.

    **Create `CreateDocumentRequest.kt`** at `android/app/src/main/java/com/gmaingret/outlinergod/network/model/CreateDocumentRequest.kt`:
    ```kotlin
    package com.gmaingret.outlinergod.network.model

    import kotlinx.serialization.SerialName
    import kotlinx.serialization.Serializable

    @Serializable
    data class CreateDocumentRequest(
        @SerialName("id") val id: String,
        @SerialName("title") val title: String,
        @SerialName("type") val type: String,
        @SerialName("parent_id") val parentId: String?,
        @SerialName("sort_order") val sortOrder: String
    )
    ```
    NOTE: The backend POST /api/documents generates its own UUID server-side. The `id` field is sent but may be ignored by the backend. Keep sending it -- no harm, and sync reconciles later.

    **NodeEditorViewModel.kt** -- 2 call sites to fix:

    1. `loadDocument()` line ~64: Change `authRepository.getAccessToken().filterNotNull().first()` to `authRepository.getUserId().filterNotNull().first()`

    2. `triggerSync()` line ~659: Change `authRepository.getAccessToken().filterNotNull().first()` to `authRepository.getUserId().filterNotNull().first()`

    **SettingsViewModel.kt** -- 1 call site to fix:

    1. `loadSettings()` line ~33: Change `authRepository.getAccessToken().filterNotNull().first()` to `authRepository.getUserId().filterNotNull().first()`

    IMPORTANT: Do NOT remove `getAccessToken()` from the interface. Do NOT change any call in the Ktor auth interceptor or LoginViewModel -- those correctly use the JWT token.
  </action>
  <verify>
    Grep to confirm no remaining `getAccessToken()` calls in DocumentListViewModel, NodeEditorViewModel, or SettingsViewModel:
    `grep -n "getAccessToken" android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModel.kt android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModel.kt android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/settings/SettingsViewModel.kt`
    Expected: zero matches.

    Grep to confirm getUserId() is now used:
    `grep -n "getUserId" android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModel.kt android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModel.kt android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/settings/SettingsViewModel.kt`
    Expected: 3+ matches in DocumentListViewModel, 2 in NodeEditorViewModel, 1 in SettingsViewModel.

    Grep to confirm snake_case in CreateDocumentRequest:
    `grep -n "parent_id\|sort_order" android/app/src/main/java/com/gmaingret/outlinergod/network/model/CreateDocumentRequest.kt`
    Expected: 2 matches.
  </verify>
  <done>
    All 3 ViewModels call getUserId() (not getAccessToken()) for Room userId queries. CreateDocumentRequest sends snake_case field names. No production code references getAccessToken() for userId purposes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all test files -- getUserId() mocks and decimal HLC fixtures</name>
  <files>
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListViewModelTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/documentlist/DocumentListSyncTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorViewModelTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorSyncTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeEditorPersistenceTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NodeContextMenuTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/NoteEditorTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/nodeeditor/DragAndDropTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/settings/SettingsViewModelTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/sync/SyncWorkerTest.kt
    android/app/src/test/java/com/gmaingret/outlinergod/ui/screen/login/LoginViewModelTest.kt
  </files>
  <action>
    Two systematic changes across ALL test files listed above:

    **Change A -- Add getUserId() mock wherever getAccessToken() was being used as userId:**

    In each test file, find lines like:
    ```kotlin
    every { authRepository.getAccessToken() } returns flowOf("user-1")
    ```
    Add a parallel line BELOW it (keep the getAccessToken mock -- LoginViewModel tests and the Ktor interceptor still need it):
    ```kotlin
    every { authRepository.getUserId() } returns flowOf("user-1")
    ```

    Specific files and what to change:

    - `DocumentListViewModelTest.kt` line ~79: ADD `every { authRepository.getUserId() } returns flowOf("user-1")` after the existing getAccessToken mock
    - `DocumentListSyncTest.kt` line ~81: ADD `every { authRepository.getUserId() } returns flowOf("user-1")` after the existing getAccessToken mock
    - `NodeEditorViewModelTest.kt` line ~133: ADD `every { authRepository.getUserId() } returns flowOf("user-1")` after the existing getAccessToken mock
    - `NodeEditorSyncTest.kt` line ~78: ADD `every { authRepository.getUserId() } returns flowOf("user-1")` after the existing getAccessToken mock
    - `NodeEditorPersistenceTest.kt`: No getAccessToken mock exists (no userId calls in tested methods). The production code calls to getUserId() happen in `loadDocument()` and `triggerSync()`, but PersistenceTest tests `onContentChanged` / `onNodeFocusLost` which don't call userId. No change needed for mock. BUT if this test does set up `loadDocument()` internally, check and add the mock if needed. Read the file -- it mocks `authRepository.getDeviceId()` but NOT getAccessToken. Since the production code's `loadDocument()` now calls `getUserId()`, and this test calls `loadDocument()`, we MUST add: `every { authRepository.getUserId() } returns flowOf("user-1")` in the setUp() method.
    - `NodeContextMenuTest.kt`: Check if it mocks getAccessToken. If not but it calls methods that now use getUserId(), add the mock. Read the file to confirm.
    - `NoteEditorTest.kt`: Same check as above.
    - `DragAndDropTest.kt`: Same check as above.
    - `SettingsViewModelTest.kt` line ~62: Change from `every { authRepository.getAccessToken() } returns flowOf("u1")` to `every { authRepository.getUserId() } returns flowOf("u1")`. ALSO keep the getAccessToken mock for completeness (add both).
    - `SyncWorkerTest.kt` line ~79: This uses `coEvery` (suspend). ADD `coEvery { authRepository.getUserId() } returns flowOf("user-1")`. Check if SyncWorker actually calls getUserId -- if not, the mock is harmless but future-safe.
    - `LoginViewModelTest.kt`: This tests LoginViewModel which correctly uses getAccessToken() to check session state. Do NOT change these mocks. LoginViewModel does NOT call getUserId(). Leave as-is.

    **Change B -- Replace hex HLC fixture strings with decimal format:**

    The hex string `"0000017b05a3a1be-0000-device-1"` (wall=0x17b05a3a1be = 1636300202430) must become `"1636300202430-00000-device-1"`.

    The variant `"0000017b05a3a1be-0001-device-1"` must become `"1636300202430-00001-device-1"`.

    Files with hex HLC strings to replace (found via grep):

    - `NodeEditorPersistenceTest.kt` line 53: `testHlcValue = "0000017b05a3a1be-0000-device-1"` -> `"1636300202430-00000-device-1"`
    - `NodeEditorViewModelTest.kt` line 52: same change
    - `NodeEditorSyncTest.kt` line 60: same change
    - `NodeContextMenuTest.kt` line 55: same change
    - `NoteEditorTest.kt` line 52: same change
    - `DragAndDropTest.kt` line 53: same change
    - `DocumentListViewModelTest.kt` line 81: `hlcClock.generate(any())` returns value, change to decimal. Also line 101: `titleHlc` fixture value.
    - `DocumentListSyncTest.kt` line 83: same change
    - `SettingsViewModelTest.kt` lines 45/47/49/51: fixture HLC values, change all. Line 64: `hlcClock.generate(any())` return value `"0000017b05a3a1be-0001-device-1"` -> `"1636300202430-00001-device-1"`

    Use find-and-replace within each file. The string `0000017b05a3a1be-0000-` should become `1636300202430-00000-` and `0000017b05a3a1be-0001-` should become `1636300202430-00001-`.

    IMPORTANT: Do NOT change `prototype/` test files (already handled in Plan 01).
  </action>
  <verify>
    Run full test suite:
    `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr" && export ANDROID_HOME="/c/Users/gmain/AppData/Local/Android/Sdk" && cd android && ./gradlew test`

    ALL tests must pass (216+ existing). Zero failures. Zero skipped.

    Also verify no remaining hex HLC fixtures outside prototype:
    `grep -rn "0000017b05a3a1be" android/app/src/test/java/com/gmaingret/outlinergod/ui/ android/app/src/test/java/com/gmaingret/outlinergod/sync/SyncWorkerTest.kt`
    Expected: zero matches.
  </verify>
  <done>
    All test files mock getUserId() where needed. All hex HLC fixture strings replaced with decimal format. Full `./gradlew test` passes with 216+ tests, zero failures.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "getAccessToken" android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/` returns ZERO matches -- no ViewModel uses getAccessToken() for userId anymore
2. `grep -rn "getUserId" android/app/src/main/java/com/gmaingret/outlinergod/ui/screen/` returns matches in all 3 ViewModels
3. `grep -rn "0000017b05a3a1be" android/app/src/test/java/com/gmaingret/outlinergod/ui/ android/app/src/test/java/com/gmaingret/outlinergod/sync/` returns ZERO matches -- all hex fixtures migrated
4. `grep -rn "parent_id" android/app/src/main/java/com/gmaingret/outlinergod/network/model/CreateDocumentRequest.kt` returns a match -- snake_case annotation present
5. Full `./gradlew test` exits 0 with 216+ tests passing
</verification>

<success_criteria>
- DocumentListViewModel.loadDocuments() queries Room with real user UUID -> document list populated correctly
- DocumentListViewModel.createDocument() sends snake_case POST body -> backend accepts without 400
- NodeEditorViewModel.loadDocument() creates root nodes with real user UUID
- SettingsViewModel.loadSettings() queries/creates settings with real user UUID
- All sync push operations use real user UUID for getPendingChanges queries
- Full test suite passes: `./gradlew test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-sync-integration-fixes/05-02-SUMMARY.md`
</output>
